#ifndef _MYINCLUDE
#define _MYINCCLUDE
#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <vector>

using namespace std;

#define SIZE_ARR (1000)

#define MAX(a, b) ((a > b) ? a : b)
#define MIN(a, b) ((a < b) ? a : b)

// Stack Array
#define TOP_SIZE	(5)
#define INF			(INT_MAX)
// Queue Array
#define QUEUE_SIZE (5)

#endif